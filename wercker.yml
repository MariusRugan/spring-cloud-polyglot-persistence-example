box: mihkels/java8-oracle@0.0.1
build:
  steps:
    - script:
        name: show base information
        code: |
          echo $JAVA_HOME
          java -version
          javac -version
          mvn -version
    - script:
        name: mvn clean package
        code: |
          mvn clean package