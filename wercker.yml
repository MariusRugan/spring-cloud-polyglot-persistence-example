box: mihkels/java8-oracle
build:
  steps:
    - script:
        name: show base information
        code: |
          echo $JAVA_HOME
          java -version
          javac -version
          mvn -version
    - script:
        name: mvn clean package
        code: |
          mvn clean package